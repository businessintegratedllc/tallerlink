{
  "name": "tallerlink",
  "version": "2.1.1",
  "private": true,
  "description": "TallerLink — taller OS + PayPal + admin",
  "scripts": {
    "start": "python3 -m http.server 8080",
    "serve": "npx --yes serve -p 8080",
    "dev": "npx --yes netlify-cli dev",
    "build": "npm install --omit=dev"
  },
  "dependencies": {
    "@netlify/blobs": "^8.2.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "UNLICENSED"
}
